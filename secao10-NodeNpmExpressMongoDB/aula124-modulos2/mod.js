//_ Dessa forma exporta diretamente uma função

//_ é similar ao default visto anteriormente.

//_ essa função não foi criada para ser executada dentro 
//_ desse arquivo

module.exports = function(x, y){
    return x * y;
};
