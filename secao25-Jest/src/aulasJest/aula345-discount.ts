/* eslint-disable @typescript-eslint/no-unused-vars */
//  * Aula 345 - Testando Discount

/*
    _Nota_
    _ por haver mais de uma classe, é importado todas.
    _ o sut recebe um construtor, e retorna um discount.
    _ como demonstrado abaixo.
    ? const createSut = (className: new () => Discount): Discount => {
    ? return new className();
    ? };
    _ e então é testado cada disconto avaliado como possível
    _ com um it separado
*/
